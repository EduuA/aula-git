const ingresso = true;
const censura = 16;
const idade = 16;


if (ingresso == true && idade >= censura) {
  console.log(`Pode entrar na sala do filme`);
} else {
  console.log(`Voce foi barrado`);
}
